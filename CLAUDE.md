# 草屋 · Caowu

> 拂春晓，秋将至

先贤所思，与今时之心遥相呼应，跨岁月而对晤，于文字义理之中相逢，至趣。

## 这是什么

一间草屋。纸白为地，茶褐为墨，谈内观，也谈营生。

- **拾韵｜省情｜凝思** —— 三个核心展示分区
- **衡几** —— 思想产出功能区（决策问答、自媒体文案输出）
- **观隅** —— 审美藏品专区（精选图片陈列、音频播放展示）

## 快速开始

无构建、无依赖、无框架。浏览器直接打开即可：

```bash
open index.html          # macOS
xdg-open index.html      # Linux
python3 -m http.server 8000   # 或起一个本地服务，访问 :8000
```

看视觉基准（色板 / 字阶 / 留白 / 动效 / 禁项）：

```
styleguide.html
```

## 目录结构

```
caowu/
├── index.html            主站：卷首 + 审美/偏好/创意 + 衡几 + 观隅
├── styleguide.html       基准样板：色 · 字 · 空 · 动 · 忌
├── styles/
│   ├── tokens.css        ★ 设计令牌——改这里即改全站
│   ├── base.css          纸墨底色、文字元素、布局骨架
│   ├── components.css    页眉、卷首、陈列、播放器、工具区、页脚
│   ├── atmosphere.css    氛围层：雨、水、远山、纸纹、雾
│   └── dock.css          侧边栏：竖排书签与听音浮层
├── scripts/
│   ├── main.js           眉滚动、缓入、导航高亮、氛围退火，衡几两件器物的接线
│   ├── library.js        曲库与藏品清单的读取（三级退让，见下）
│   ├── player.js         播放器：曲目轮播、底部听音条、陈列渲染
│   ├── hengji.js         衡几本体：决策问答的五问、文案骨架（本地，不联网）
│   ├── ambient.js        听音：WebAudio 合成的雨声与流水（真曲目的后备）
│   ├── dock.js           侧边栏浮出、声音总闸、音量联动
│   ├── rain.js           canvas 雨丝
│   ├── gen-manifest.py   扫 assets/ 生成 assets/manifest.json（加文件后跑一次）
│   └── sync-knowledge.py 把 vault-template 知识库同步成 worker/src/knowledge（改典籍后跑）
├── worker/               衡几先生（Cloudflare Worker，独立部署，见 worker/README.md）
└── assets/
    ├── manifest.json     ★ 自动生成，别手改
    ├── README.md         怎么补标题、说明、比例
    ├── img/              藏品图片放这里
    └── audio/            音频放这里
```

## 设计约束（不可协商）

来自 `~/.claude/CLAUDE.md`，落到代码里就是下面这些。

### 色

米白、淡牙白为底；茶褐、浅墨灰为字；灰青、浅赭石作点缀。

**禁止**：高饱和亮色、霓虹、重古风大红大绿。

改色只改 `styles/tokens.css` 顶部的变量，不要在组件里写死色值。

实测对比度（**真实浏览器渲染后逐元素采样**，不是按纯色推算）：

氛围层（渐变 + 雾 + 山影）会把纸底压深。直接坐在纸底上的文字，
实际底色是 `#F1EEE6` 上下；卷首因雾与冷色渐变叠加最沉到 `#EAE7DF`。
下表的「最沉处」就是按这些实测底色算的，是保守值。

| 令牌 | 值 | 纯纸底 | 最沉处 | 用途 |
|---|---|---|---|---|
| `--ink` | `#362D24` | 12.4:1 | 10.9:1 ✅ | 正文、标题 |
| `--ink-soft` | `#4A3F34` | 9.4:1 | 8.3:1 ✅ | 次级正文、按钮 |
| `--ink-faint` | `#574C41` | 7.7:1 | 6.8:1 ✅ | 分区说明、卡片正文、注释 |
| `--ink-wash` | `#6A6055` | 5.6:1 | 5.0:1 ✅ | 眉标、落款、占位符 |
| `--qing` | `#35544F` | 7.6:1 | 6.7:1 ✅ | 链接、点缀 |
| `--zhe` | `#8A5433` | 5.7:1 | 5.0:1 ✅ | 短横、强调、印章 |

四阶墨色保持亮度单调递增（越往下越浅），改值时别破坏这个次序。

**别用 `opacity` 把文字压淡**。这一版踩过两次：印章 `opacity: 0.72` 的赭石
实测掉到 3.2:1，卷首竖排那句 `--ink-wash` 乘 0.62 只剩 2.5:1 ——
两处都是「看上去只是淡了点」，实则已经不成字。要更淡就换更浅的墨色令牌，
或者干脆不用那行字。装饰性的面（短横、边框、氛围层）压透明度没问题，
只要它不承载文字。

改完令牌的验收口径：**全站逐元素实测，最低一处不低于 4.5:1**。
测量脚本见 `notes/T12.31.md` 末尾，或照下面的方法自己写一个。

**校验方法**：改完令牌不要靠算，跑一次真实渲染重新采样——

1. 把页面里目标文字的前景色临时刷成 `transparent`，其余全部保留
2. 截该元素的精确矩形
3. 取矩形内的**众数色**作底色（`-colorshift` 不必，直接统计像素频次）
4. 用 WCAG 相对亮度公式算比值，与 4.5:1 比

注意两个坑：一是别用「矩形内最沉像素」，那常是相邻元素渗进来的字；
二是别把卡片自己的底色也一起藏掉，否则量到的是卡片背后的山影，
会得出假的低值。

### 字

仿宋 / 思源宋体类儒雅字体。正文 `--font-song`，注释与引文 `--font-fang`。

字距是宋式书册感的一半来源：正文 `0.06em`，眉标 `0.34em`。

### 空

留白优先，疏朗排版，仿宋式书册布局。线条细弱（`--line-soft` 默认），少装饰。

正文行宽守 `--measure`（34rem，约 30 字）——超过 35 字阅读即费力。

### 动

简约柔和的流动效果。轻量渐变、缓慢过渡。

**禁止**：爆炸动画、复杂粒子。动效服务留白，不抢夺内容焦点。
时长上限 1.2 秒。已实现 `prefers-reduced-motion` 降级。

唯一的例外是**氛围层**（雨丝、水波、雾、颗粒）：这些属背景呼吸，
一轮在 8 秒以上，速度慢到不被察觉，`prefers-reduced-motion` 时全部静止。

### 氛围与听音

雨、水、远山是**背景**，不是装饰：

- 全部收在 `.atmos` 一层里（`position: fixed`、`pointer-events: none`、`inert`），
  任何内容都压在其上，不影响选中、点击、朗读
- 雨丝的上限按屏宽给（26 / 46 / 78 条），30fps，切走标签页即停笔
- **声音有两条来源，同一套规矩**：
  - `scripts/player.js` —— 真曲目，读 `assets/manifest.json`，播完接下一首
  - `scripts/ambient.js` —— 合成雨声，WebAudio 实时生成，零体积可离线。
    没有曲目文件时它就是全站的声源
- 曲目起声时合成层**让位**（`Ambient.duck()`），不是压小，是退到底；
  曲目停才回来（`unduck()`）。雨声与曲目共用一路音量
- 侧栏那一枚按钮是**总闸**：谁在响就先关谁，都没响就把上次开着的那路接回来
- 声音**默认不响**。点过按钮才起，选择记在 `localStorage`
- 底部听音条是全站唯一一处固定控件，**平日藏在屏下**：桌面端鼠标探到屏幕底缘才浮出、
  移开即收回（键盘焦点在条内时留住）；触屏没有悬浮，播放时常驻浮出。页脚不预为它留白

### 陈列与曲库

网页列不了目录，GitHub Pages 也不会，所以「读目录」落在构建期：

```bash
python3 scripts/gen-manifest.py     # 加了图/音频之后跑一次
```

它写 `assets/manifest.json`，页面（`library.js`）读它。三级取材逐级退让：
清单 → 本地目录列表 → 内置占位。清单**空着**也算合格答案——
「还没上传」不该被猜成别的东西，界面就老实地空着。

图片与音频的标题、说明、比例写在各自目录的 `_meta.json` 里，别改 HTML。
详见 `assets/README.md`。

陈位按图的形状给，不硬塞：宽高比 > 2 走横长陈位（21:9），< 0.85 走竖陈位（3:4），
其余 4:3。第一幅默认给横长位。清单脚本读图头判比例，HTML 里不写。
`#gallery` 用 `grid-auto-flow: dense` —— 横长幅占满一行后，
后面的小幅会回身把行尾补上，整页不出豁口。

个别画幅可在 `_meta.json` 用 `frame`（画心宽高比，优先于图头 `ratio`）
与 `pos`（CSS `object-position`）微调陈位，如远山收高到 1.7、贴底对齐，
让一线山体抬上来——这是例外口径，只给确实需要的画幅，别变成惯例。

### 侧边栏

左缘一条竖排书签，**滚过卷首才浮出**——卷首是一整页留白，不该被控件打扰。
窄屏（≤60rem）改为底部横胶囊。触屏没有 hover，胶囊滚过卷首后本会常驻，
但它会压住画心与衡几按钮——故窄屏**滑动中胶囊收起**（`is-napping`）：没放声整条
沉回屏下，正在放声只留一枚呼吸点（`:has(.is-playing)`，老浏览器降级为整条隐去），
停手 0.6s 浮回；焦点在输入字段（写衡几）或胶囊内、曲目卡展开时不收。
宽屏竖排书签不受这套影响。

### 衡几

两件器物**不存任何东西、关掉页面即散**，但跑法不同：

- **决策问答**是一套问法，不是答案，**纯本地不联网**。五问按次序：
  目标 → 代价 → 最坏能不能承住 → 不做会怎样 → 十年后。
  第三问答「承不住」当场收尾，不再往下问。收尾只摆出你说过的话、
  指出自相矛盾处、给最后三问，**不替你下判断**。
- **文案输出**由衡几先生代笔：页面把题目发给 Cloudflare Worker（`worker/`），
  后端保管 `DEEPSEEK_API_KEY`、内置知识库（心理问题总表＋儒释道典籍）、
  用 Tavily 联网查近期时事，三重印证后出稿。**密钥只在 Worker，不进页面、不进仓库。**
  Worker 若停了，前端自动退回本地**骨架**（起承转合各段做什么、忌什么），
  草屋不假装有智能，也不因为后端没了就空着——「十年后还能用」靠这层降级保底。

接线在 `main.js`：决策区题面与答复共用一个 `textarea`，靠闭包里的 `state` 区分。
按钮一个两用（`开始追问 → 下一句 → 再问一件`），**空答复框点击不推进**。
对话状态**不进 `localStorage`** —— 自省的话不该留在机器上。
文案区一次成稿：等待时按钮禁用显示「先生正在想…」，时事来源以淡墨小字另列
（`.output-meta`），不混进正文。

### 气质

沉静内观，儒家中正，兼顾世事思考与务实营生。

**拒绝**：浮夸拜金、玄幻堆砌、元素堆砌。文字克制清淡，文人感。

## 待办

- [x] **听感确认**：十首曲目 2026-09-19 人耳听过（数据层此前已验：请求 200、
      `currentTime` 在走、音量联动生效）
- [x] 新图陈位线上验过：`03-雾林双檐` 两只檐尖都在；松影、夜灯残墨、静瓶朱枝
      经用户拍板统一为 3:4 竖陈位（`tall` + `frame`）
- [ ] 若日后文案卡（`#copy-output`）被撑过一屏，照 `#decide-output` 的办法封高
- [ ] 若内容变多，再考虑拆多个页面或引入静态生成器——目前刻意保持单页
- [ ] 衡几智能体（`agent/`）：healing-scribe 首版已入仓（DSH preset，提示词母本）；
      vault-template 时事真实条目待补；另待吊销其 URL 内嵌的旧 token
- [ ] 衡几先生 Worker（`worker/`）：代码与逻辑测试已过，待 `wrangler login` + 两个密钥后
      deploy，回填地址到 `scripts/hengji.js`，再线上实测出稿

## 为什么是纯静态

内容量级小；「极简、留白、少依赖」是调性本身，不只是实现手段。
零构建意味着十年后打开这堆文件它还能跑，这对一间草屋是合适的寿命。
