# A08.55 · 会话进度

**时间**：2026-09-26 08:55 CST 起（周六）
**工程**：`/root/workspace/caowu`（草屋）
**本次一事**：白狐桌宠从 fox-brain-web 独立版并入草屋主站，公网可访问。

承接 fox-brain-web Day2（独立项目：Node 代理 8787 + 三态白狐，
真实素材已去白底，睡态已不压暗）。

---

## 一、做法

1. **Worker 新增路由 `POST /fox/chat`**（`worker/src/index.js`）
   - 原 Worker 只在 `/` 靠 body.type 分流；现按 `new URL().pathname` 加分支。
   - `handleFoxChat()` 把 `{messages}` 转发火山方舟 **Anthropic 兼容入口**
     `https://ark.cn-beijing.volces.com/api/coding/v1/messages`，
     x-api-key 用已有的 `ARK_API_KEY` secret，model 填 claude-sonnet-4-5（方舟映射），
     注入小狐人设与工具声明（新文件 `worker/src/prompt-fox.js`），透传 JSON。
   - CORS/OPTIONS 沿用全局（白名单已含 sevencolor.space，另放行 localhost）。
2. **主站前端**
   - 素材在 `assets/fox/fox_{idle,wave,sleep}.png`（gen-manifest 只扫 img/audio，不进展厅）。
   - `styles/fox.css`：移植独立版样式，颜色全部走 tokens 令牌；
     位置 `right:2rem; bottom:14vh`（右侧中间稍下），z-index 35；
     ≤40rem 狐狸缩 120px、面板左右留边。
   - `scripts/fox.js`：移植独立版 app.js，API 指向
     `https://hengji.sevencolor.space/fox/chat`；天气/提醒/TTS 仍在浏览器端。
   - `index.html`：加引 css/js 与狐狸、面板标记。
3. `.gitignore`：忽略 IB/（上传暂存）与 notes/shot-*.png（验证截图）。

## 二、验证

- Worker deploy 成功（Version 58b16558）。
- `/fox/chat` curl：普通对话 200、CORS 头正确；问杭州天气 → tool_use get_weather。
- 本地 8801 无头截图：狐狸位置、面板展开、气泡样式均合（截图本地留档未入库）。

## 三、没做完的

- 用户尚未在 https://sevencolor.space 肉眼实测（单击面板、天气、提醒、语音）。
- 路由可被 curl 直接调用消耗方舟额度；日后可加 Cloudflare 速率限制。
- 方舟 key 在会话记录里暴露过，待用户轮换。

## 四、下一步（接手先看这里）

1. 等用户线上实测反馈，**按序号逐条改、逐版看效果**。
2. 再动 Worker：凭据在 `worker/.env`，
   `cd worker && npx -y wrangler@latest deploy --env-file .env`。
