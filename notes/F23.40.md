# F23.40 · 会话进度（部署）

**时间**：2026-09-18 23:48 CST
**工程**：`/root/workspace/caowu`（草屋）
**本次一件事**：把 `F23.10` 那批改动推上 GitHub Pages，并在线上验一遍

承接 `F23.10`。那个文件末尾写着「三笔未推，推送与否由用户决定」——用户让推了。

---

## 一、推送：成了 ✅

```
cfd2f87..97cfa80  main -> main
```

`F23.10` 记的两笔积压 + 本次一笔，再加一笔补状态的提交，**四笔一起上去**。

线上 HEAD（`gh api` 复核）：

```
gh api repos/magicwannago-lang/caowu/pages/builds → .[0].commit = 97cfa80  status = built
```

**注意**：推送后 `git ls-remote` 再试又断了（`GnuTLS recv error (-110)`），
但本地 `git rev-list --count origin/main..HEAD` = **0**，说明推已生效
——**本地的 `origin/main` 引用是 `push` 成功时更新的，比 `ls-remote` 可信**。
这一条记下来：网络不通时不要靠 `ls-remote` 判断推没推成。

---

## 二、Pages 构建：等了一会儿

推送后线上 `manifest.json` **一度仍是旧的**（只有 2 幅图），
`gh api .../pages/builds` 显示 `building`。约 1 分钟后转 `built`。

**经验**：`git push` 成功 ≠ 线上已更新。**要看 `pages/builds` 的 `status`，
别靠反复 curl 猜**。

---

## 三、线上验收 ✅

`https://sevencolor.space`，逐项实读（不看本地文件）：

### 清单：六幅图全在，陈位标记正确

```
音频 3 首，图片 6 幅
  01-远山.jpg      wide=True   tall=False   title=远山
  02-疏影.jpg      wide=False  tall=True    title=疏影
  03-雾林双檐.jpg  wide=True   tall=False   title=雾林双檐
  04-松影.jpg      wide=False  tall=False   title=松影
  05-夜灯残墨.jpg  wide=False  tall=True    title=夜灯残墨
  06-静瓶朱枝.jpg  wide=False  tall=True    title=静瓶朱枝
```

竖陈位（`tall`）三幅：`02`、`05`、`06`。与本地跑出来的归位一致。

### 资源 HTTP 码：全部 200

| 资源 | 码 |
|---|---|
| `/`（首页） | 200 |
| `/styles/components.css` | 200 |
| `/assets/favicon.svg` | 200 |
| `/scripts/main.js`（含衡几接线，实读命中 7 处） | 200 |
| 六幅图 `01`–`06` | 全 200 |
| 三首曲 `.mp3` | 全 200 |

**所以 `F23.10` 里那四件事在线上都生效了**：衡几能点、六幅图能显、
竖幅不被裁、favicon 不再是 404。

---

## 四、**还没验的**（明天接着看）

线上 HTTP 层是通的，但**渲染后的样子没在真浏览器里看过线上版**。
本地 headless Chromium 验的是本地文件，线上这版只验到了「文件都在、码都对」。

要用眼睛过的三处：

1. **`03-雾林双檐` 的 21:9 会不会裁掉两只檐尖** —— 这是 `F23.10` 待办里点名的一条。
   1600×500 裁进 21:9 只掉极窄一条，风险不大，但要看一眼。
2. **`06-静瓶朱枝` 的朱红饱和度** —— `F23.10` 里提过，它比全站其他图高一档，
   在满版米白里会比较跳。要不要压一压，得看过整页才有判断。
3. **衡几在线上真点一遍** —— 本地验过逻辑，线上要先确认 `main.js` 加载顺序
   与 `localStorage`（前几次访问可能存了旧的听音状态）不出岔子。

另外仍然是那条**动不了的**：**听感确认**（三首曲是否耐听、雨声与曲目接得顺不顺）
——要人耳，我这边听不了。

---

## 五、进度

| 项 | 之前 | 现在 |
|---|---|---|
| 衡几接逻辑 | ✅ 已接（本地验） | ✅ **线上生效** |
| 藏品六幅 + 题款 | ✅ 本地 | ✅ **线上生效** |
| 竖陈位（`tall`） | ✅ 本地 | ✅ **线上生效** |
| favicon | ✅ 本地 | ✅ **线上生效** |
| 线上页面观感（眼过） | — | ⏳ **待看**（见第四节三处） |
| 听感确认 | 待办 | **待办**（人耳） |

---

## 六、下一步（明天）

- [ ] 线上用眼睛过一遍：`03` 的檐尖、`06` 的朱红、衡几点一遍
- [ ] 听感确认（人耳）
- [ ] `F23.10` 留的：若文案卡被撑过一屏，照 `#decide-output` 的办法封高

---

## 附：仓库状态

```
工作树：干净
main：与 origin/main 一致（ahead 0）
线上：97cfa80，Pages built
```

```
97cfa80  notes：F23.10 补提交状态（三笔未推）
f97aaba  衡几接线 + 藏品四幅入册 + 竖陈位
5217bd4  notes：D17.30 补提交状态（已提交未推送）
c45c98a  藏品题款 + favicon + 衡几逻辑（本地，无后端）
```

无残留测试文件（`drive.js` / `_test.html` 已删）。
