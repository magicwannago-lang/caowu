# J16.12 · 会话进度（三小幅统一竖陈位；歌词销账；听感勾讫）

**时间**：2026-09-19 16:12 CST
**工程**：`/root/workspace/caowu`（草屋）
**本次一件事**：用户看完 I15.55 的线上版，给了三条意见，逐条落地。

---

## 一、用户意见与处理

1. **「静瓶朱枝、松影的尺寸都和夜灯残墨一致」** → 三小幅统一 3:4 竖陈位。
   改动只在 `assets/img/_meta.json`（+ 重新生成的 manifest）：

   | 幅 | 原陈位 | 现陈位 | 取画心 |
   |---|---|---|---|
   | 02 松影 | 4:3（331×248，按图头 1.333） | **3:4 竖（331×441）** | `tall:true` + `frame:0.75` + `pos:"63% center"`——窗口右移，大树（主角）完整，左下小树也保住，左山让成影子 |
   | 04 夜灯残墨 | 3:4（331×441） | 不动 | — |
   | 05 静瓶朱枝 | 1:1 方（331×331） | **3:4 竖（331×441）** | `scale:true` 改 `tall:true` + `frame:0.75` + `pos:"right center"`——贴右取，白瓶与花枝完整 |

   三幅现在同顶线、同尺寸，与远山（横长）、雾林双檐（横长）两行横陈位之间，
   夹一行齐整的竖陈位。本地截图 `/tmp/local-three.png` 验过。

2. **「歌词先不上」** → G13.20 审美待决第 1 条销账。三份 `.lrc`
   （含头两行 JSON 残留的 `Coce - Person.lrc`）继续零引用躺着，不删——
   日后想上，机制笔记在 H14.55 第五节。

3. **「听过了」** → 听感确认勾讫（CLAUDE.md 待办已勾）。无负面反馈，
   G13.20 第 3 条「静瓶朱枝朱红压不压」随本次竖陈位一并落地后销账：
   竖幅里朱仍只在花上，褐墙为主，不压。

## 二、顺手一处脚本诚实性修正

`gen-manifest.py`：点名陈位与图头比例不符时会打 stderr 提醒
（「标为 tall，实际宽高比 1.33——已按实际比例排，未裁」）。
**显式给了 `frame` 时这句是假话**（本就是摆明要裁），条件改为
`not item.get("frame")`——本次生成输出干净，无提醒。

## 三、验证口径

本地 `http.server 8123` + headless（强制 eager、瞬时跳转、视口截图，
套路见 I15.55 第二节）：

```
01 远山    relic-wide   1060x624  object-position 50% 100%
02 松影    relic-tall    331x441  object-position 63% 50%
03 雾林双檐 relic-wide   1060x331  50% 50%
04 夜灯残墨 relic-tall    331x441  50% 50%
05 静瓶朱枝 relic-tall    331x441  100% 50%
```

三小幅 `top` 同为 4516。松影树冠右梢贴边但完整；
静瓶瓶身、木案、顶部花枝都在框内。

## 四、仓库状态

本次改动：`assets/img/_meta.json`、`assets/manifest.json`（生成物）、
`scripts/gen-manifest.py`（提醒条件）、`CLAUDE.md`（待办勾讫）。
提交推送后补记线上 build 号。
