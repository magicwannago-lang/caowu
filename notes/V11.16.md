# V11.16 · 会话进度

**时间**：2026-09-21 11:16 CST（周一）
**工程**：`/root/workspace/caowu`（草屋）
**本次一件事**：开发衡几「自媒体文案输出」智能体 healing-scribe：读文件评估可用性 → 补提示词 → 配置可换大模型

---

## 接手时现状

`agent/` 整个目录未入 caowu git。内含：

- `healing-scribe/`：人设 + Obsidian MCP + 网页检索 + 一个工作流 skill，骨架对，但
  - 提示词没指向心理问题总表（总表实际在 `vault-template/emotion/心理自媒体/心理自媒体目标.md`）；
  - 典籍库只有《道德经·第八章》一篇，无典可引；
  - 时事只有格式示例；
  - 没有模型配置。
- `decision-advisor/`：决策问答 preset（用户说对应衡几决策问答，本次未改其内容）。
- `vault-template/`：**独立 git 仓库**（remote 指向 magicwannago-lang/vault-template.git）。

DSH 模型写法是现查 npm 确认的：`@deepseek-ai/dsh-agent-default-model`，
插件配置 `{ provider, model }`；DeepSeek 官方适配器路由 `deepseek-official`，
模型 `deepseek-v4-flash` / `deepseek-v4-pro`，Key 走 `DEEPSEEK_API_KEY`。

## 逐件做法

### 1. vault-template 知识库（独立仓库，commit 5a26753）

- `emotion/心理自媒体/心理自媒体目标.md` → `心理问题/现代人心理问题汇总.md`（git mv），
  加 frontmatter（tags 覆盖六大板块）与「给智能体」的使用说明；正文原文保留。
- 补典籍九篇（只收确信准确的名句，格式同原有第八章笔记）：
  - 儒家：论语 / 孟子 / 大学 / 中庸
  - 道家：道德经选章（2/16/22/33/44/63/76/78 章）/ 庄子
  - 佛家：心经 / 金刚经 / 六祖坛经
- `时事/README.md`（命名、格式、联网退让口径）、`治愈文案/README.md`（归档约定）。
- 删空文件与 Obsidian 欢迎页（undefined、未命名 x2、.base、.canvas、欢迎）。
- 根 README 按实际结构重写。
- `emotion/自得.md` 等建库者原始手记保留，未动。

### 2. healing-scribe（commit e8e1ebf）

- **人设重写**：工作流定为
  定题（定位总表条目/随机抽取）→ 澄清（体裁/受众/语气/条数）→ 取典 → 时事
  （库内优先、不足联网、不许拿示例冒充）→ 三重印证 → 成稿 → 询问归档。
- **草屋写作规矩**：先承认痛苦再给出路；不鸡汤、不口号、不硬升华；
  克制清淡有画面；术语点到即止；不玄幻不拜金；每篇有一个可落地的日常落点。
- **三家分际**：儒=关系担当 / 道=放下顺其自然 / 佛=觉察慈悲；一家为主不硬凑。
- **危机边界**：自伤自杀信号停止创作，引导专业援助（12356）。
- **模型块**：`agent-default-model`，provider/model 两行可换，注释写明可选项与换家条件。
- **SKILL 重写**：体裁规格表（公众号 600–900 / 朋友圈小红书 150–300 / 卡片金句 20–60）、
  三条多条规则（角度各异）、输出模板（出处不进发布稿）、按库存笔记列的典籍速查表。
- README 同步：模型配置、`DEEPSEEK_API_KEY`、知识库现状（时事待补）。

### 3. decision-advisor

内容未改，首次入 caowu 仓库（其 skill 在 `skills/task-analysis/SKILL.md`）。

## 收尾（用户已拍板并执行）

- 两个仓库都已 push main。
- vault-template 保持独立，已加进 caowu `.gitignore`（commit 8968d8f）。

## 没做完的

- [ ] **安全**：`vault-template/.git/config` 的 remote URL 里明文带 GitHub token
      （`ghp_Ijzy…`，目前仍有效，push 走的就是它）。建议尽快到 GitHub 吊销重签，
      再把 remote 改成不带 token 的 URL、凭据交给凭据管理器。
- [ ] 时事真实条目等用户后续补充；补之前智能体靠网页检索。
- [ ] 未实跑验证（本机未装 DSH / uvx），仅做了 YAML 解析校验。

## 下一步

1. 用户在 DSH 里实跑一轮（建议「随机抽一条，写三条短文」），按序号给修改意见，再迭代提示词。
2. 顺手吊销并更换 token。
3. 视使用反馈再补典籍（如《传习录》、更多《庄子》篇）与时事条目。

## 提交状态

| 仓库 | commit | 远端 |
|---|---|---|
| caowu | `8968d8f` 智能体首版 + gitignore + notes | 已 push main |
| vault-template | `5a26753` 知识库整理 | 已 push main |
