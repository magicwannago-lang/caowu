# X22.21 · 会话进度

**时间**：2026-09-21 22:21 CST（周一）
**工程**：`/root/workspace/caowu`（草屋）
**本次一件事**：优化 decision-advisor 的 `system_prompt.md`——7 种决策类型各自带上工程经验式的具体追问

承接 W12.36（衡几先生 Worker 已上线待实测）。本轮不碰页面与 Worker。

## 起因

用户上传/指认 `agent/decision-advisor/system_prompt.md`：原稿是通用的
「7 类型 + 9 固定维度」，维度只到标签级（如"预算/资金：一次性支出、隐性成本"）。
用户要求：**决策类型要能像工程经验那样提出具体问题**，余地交我。

## 现状里的一处不一致（未处理，待用户定）

- `agent.cordis.yml` 的实际人设＝谋事参谋＋《工程经验.md》母本＋八大维度＋宗旨；
- `preset.yml` 描述与 cordis 一致；
- 但 `system_prompt.md` 原是另一套独立框架。本轮已把 system_prompt 向谋事参谋身份靠拢，
  cordis 未动。

## 改动（仅 `agent/decision-advisor/system_prompt.md`，全文重写）

1. 身份改为草屋衡几「谋事参谋」，纳入三条宗旨：时间固定、减少责任、精力有限。
2. 7 种决策类型保留为骨架，每种配四件东西：
   - 识别信号（1–2 条判据）
   - **必问清单（5 问左右，老手口吻、要求量化/书面凭证）**——本轮核心
   - 常见坑
   - 应对手法（一票否决、权重先定、缓冲金、尾款杠杆、可逆小步、最后反悔时点等）
3. 原 9 维度保留为「通用兜底九问」，由标签改写成具体问句。
4. 输出仍 8 段；推荐结论增「用户工作主体定位」「责任转移要点」；
   评估表要求命中清单逐项入表，缺口标【待确认】。
5. 执行规则：判定类型后一次性挑 3–5 个最关键问题问清，不反复追问；
   事实（经验库）与推断分开；每条建议自问占多少时间、背多少责任。

## 反馈一（同日 22:2x）：只有一问，一轮直出

用户明确交互逻辑＝**判断问题类型 → 直接给出面临的问题**，只有一轮，不反问。
已改 system_prompt：

- 开头角色定义写明「交互只有一轮，不反问、不追问」。
- 删原「第一步/第二步」，改「唯一流程」：判类型 → 命中清单逐项回应 → 兜底九问 → 一次输出。
- 信息缺口处理：按常理做**显式假设**并标【待确认】，写出「假设若不成立结论如何变化」。
- 输出第 2 段改为「前提假设 / 核心目标与硬性门槛」。
- 执行规则第 1 条由「先问再判、一次性问清」改为「一轮直出，绝不反问」。

## 同步落地（同日 22:3x，用户：「按照最新的改动」）

把单轮直出框架同步进实际生效的组装与文档：

- `agent.cordis.yml`：头注释改写；persona 全文换成 system_prompt 同版（宗旨＋
  7 类型必问清单＋兜底九问＋专项审查＋唯一流程＋8 段输出＋原则，紧凑 2.5k 字）；
  **移除 tool-ask-user 声明**；其余（模型、Obsidian MCP、web、skills、todo）不动。
  YAML 经 Python 解析通过（!!js 自定义构造器），8 个组装项。
- `preset.yml`：description 改为「一轮直出、判 7 类型、必问清单、不反问」。
- `README.md`：首段、「八大维度」整节改为「七种决策类型与必问清单」、使用示例流程对齐。

## 收尾（用户：删掉后 push、上线）

- 删除 `skills/task-analysis/SKILL.md` 及空 skills 目录（旧框架遗留，git rm -f）。
- commit `278373f` 已 push main（6 files：＋308 −181），GitHub Pages 随 main 自动部署。
- 说明：本次改动只涉及 agent 提示词与 notes，**页面本身无变化**。
- preset 本机同步：查明**此沙箱未装 DSH**（无命令/无 preset 根/无 registry 凭据），
  新增 `agent/install-presets.sh`：一键把含 agent.cordis.yml 的目录同步到
  `${DSH_HOME:-$HOME/.dsh}/.agent-presets/`；临时 DSH_HOME 实测同步 2 个 preset 通过；
  README 安装段已更新。用户需在跑 DSH 的机器上执行该脚本。
