# Y23.25 · 会话进度（明日继续）

**时间**：2026-09-21 23:25 CST 起（周一）
**工程**：`/root/workspace/caowu`（草屋）
**本次一件事**：把线上「决策问答」从本地五问多轮改为**谋事参谋一轮研判**（Worker），不在则退本地必问清单

承接 X22.21：当时「一轮直出、七类型必问清单」只落到 DSH 智能体，页面与 Worker 未动。
用户发现「线上的决策问答还是五问」，本轮补网页。路线经用户拍板：走 Worker。
用户确认：**决策不关联时事**（已落实——decision 分支在 searchNews 之前返回，也不拼典籍）。

---

## 一、代码改动（已完成，未提交，明日与 Worker 域名改动一起 commit）

1. **Worker 新增 decision 路由**
   - 新 `worker/src/prompt-decision.js`：决策提示词，与
     `agent/decision-advisor/system_prompt.md` 同源；删去《工程经验.md》知识库那条。
   - `worker/src/index.js`：请求体识别 `type:'copy'|'decision'`（缺省 copy，兼容旧调用）。
     decision：不查 Tavily、不拼典籍、temperature 0.3、max_tokens 6144、
     **thinking disabled、stream:true，SSE 原样透传**；copy：维持原流程，也加了
     thinking disabled。LLM 超时 90s→110s。
   - `worker/README.md`：接口示例、流式与 thinking 说明已更新。
2. **前端一轮直出**
   - `scripts/hengji.js`：删除五问多轮全套；新增 `advise(text,onChunk)`（读 SSE，
     逐块回调；空结果/非 ok 抛错）与 `decisionFallback(text)`（本地词面粗判七类型、
     可多选，认不出归权衡型；输出类型必问清单＋兜底九问，标明无研判）。
   - `scripts/main.js` 决策接线重写：一轮、等待态「参谋正在想…」、边收边渲染、
     「再问一件」＋「重来」；删 state/textarea 两用/进度 note。
   - `index.html`：按钮「开始追问」→「请参谋研判」，说明、占位、底注改写。
   - `styles/components.css`：删除不再使用的 `textarea.field.is-answer`。
3. `CLAUDE.md`：衡几节、接线说明、待办已同步为新架构。

工作区当前未提交改动：上述 7 个 M 文件 + `worker/src/prompt-decision.js` 新文件 + 本笔记。

---

## 二、排障结论（重要）

- 现象：Worker 部署成功后，两个路由 curl 均挂 100s+ 无响应。
- 定位：方舟直连正常（小提示词 2.7s；完整决策题非流式 160s，其中 7075 token 是 reasoning）。
  → V4-Pro 是推理模型；加 `thinking:{type:'disabled'}` 后 **65s 出齐、0 推理 token、报告合规**；
  方舟流式**首字秒回**。
- **根因：`*.workers.dev` 在本网络（国内）被 DNS 污染**——
  `caowu-healing.magicwannago.workers.dev` 被解析到 Facebook 假 IP
  （`2a03:...:face:b00c:...` 及 31.13.71.19），TCP 30s 超时，HTTP 000。
  api.cloudflare.com 正常。**国内用户浏览器调 workers.dev 同样不通**——
  不换域名，线上决策/文案永远只能走本地降级。

## 三、明日待办（需用户在网页控制台操作，Worker token 只有部署权限）

现状：Cloudflare 里**已存在 `sevencolor.space` 的 zone**（id `3605b7a41aeaab630699846b3437b7d5`，
状态 `initializing`），分配 NS：`abby.ns.cloudflare.com` / `phil.ns.cloudflare.com`。
现 NS 在阿里云万网（vip1/vip2.alidns.com），域名疑似阿里云注册。

1. **核对 CF DNS 记录**（dash 该 zone → DNS；全部灰云 DNS only），与阿里云一致：
   - A `@` 四条：185.199.108.153 / .109 / .110 / .111
   - CNAME `www` → magicwannago-lang.github.io
   - TXT `@` → `_github-pages-challenge-magicwannago-lang.sevencolor.space`
   （无 MX，迁移零风险；token 无 DNS 读权限，只能用户看）
2. **注册商改 NS**：阿里云域名控制台把 vip1/vip2.alidns.com 改为
   abby.ns.cloudflare.com / phil.ns.cloudflare.com；等 zone 变 Active（十几分钟–数小时）。
3. **Worker 挂自定义域名**：CF → Workers & Pages → caowu-healing →
   Settings → Domains & Routes → Add Custom Domain：`hengji.sevencolor.space`。
4. 完成后我这边：
   - 改 `scripts/hengji.js` 的 HEALING_API → `https://hengji.sevencolor.space`
     （worker README、CLAUDE.md 地址一并回填）
   - curl 实测流式决策首字时间＋文案回归
   - commit 全部改动 push main（Pages 自动部署），请用户页面实测，按反馈序号迭代

## 备注

- wrangler tail 也因污染/出口问题连不上（ETIMEDOUT Facebook IP、需 DO_NOT_TRACK=1）。
- worker 凭据在 `worker/.env`（CLOUDFLARE_API_TOKEN / ARK_API_KEY / TAVILY_API_KEY）；
  部署：`cd worker && npx -y wrangler@latest deploy --env-file .env`（生产部署，用户跑或显式授权）。
